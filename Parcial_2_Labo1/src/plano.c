/*
 * plano.c
 *
 *  Created on: 28 jun 2022
 *      Author: Usuario Programador
 */
/*

		if(contFClass!=0 && (controller_saveAsText("C:\\Registros\\Ordenados\\Listado FirstClass.csv",pArrayListPassenger)==RETORNOPOSITIVO))
		{
			retorno=RETORNOPOSITIVO;
			printf("\nSe ha generado un nuevo archivo de ordenamiento para FirstClass. Hay un total de %d",contFClass);
		}
		else
		{
			puts("\nNo se encontraron pasajeros FirstClass");
		}

		if(contExClass!=0 && (controller_saveAsText("C:\\Registros\\Ordenados\\Listado ExecutiveClass.csv",pArrayListPassenger)==RETORNOPOSITIVO))
		{
			retorno=RETORNOPOSITIVO;
			printf("\nSe ha generado un nuevo archivo de ordenamiento para ExecutiveClass. Hay un total de %d",contExClass);
		}
		else
		{
			puts("\nNo se encontraron pasajeros Executive Class");
		}

		if(contEcClass!=0 && (controller_saveAsText("C:\\Registros\\Ordenados\\Listado EconomyClass.csv",pArrayListPassenger)==RETORNOPOSITIVO))
		{
			retorno=RETORNOPOSITIVO;
			printf("\nSe ha generado un nuevo archivo de ordenamiento para EconomyClass. Hay un total de %d",contEcClass);
		}
		else
		{
			puts("\nNo se encontraron pasajeros EconomyClass");
		}
*/
